<footer class="site-footer">
	<div class="container footer-inner">
		<div class="footer-brand">
			<p class="kicker">Lumen Studio</p>
			<p class="footer-copy">
				Prendas básicas de moda minimalistas con una experiencia de compra refinada.
			</p>
		</div>

		<div class="footer-links" aria-label="Footer">
			<a href="/productos">
				<span class="footer-icon" aria-hidden="true">
					<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
						<rect x="3.5" y="3.5" width="7" height="7" rx="1.5" />
						<rect x="13.5" y="3.5" width="7" height="7" rx="1.5" />
						<rect x="3.5" y="13.5" width="7" height="7" rx="1.5" />
						<rect x="13.5" y="13.5" width="7" height="7" rx="1.5" />
					</svg>
				</span>
				Catálogo
			</a>

			<a href="/productos/camisa-lino-oversized">
				<span class="footer-icon" aria-hidden="true">
					<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
						<path d="M7 4.5 12 7l5-2.5 2.5 4L16.5 11v8h-9v-8L4.5 8.5z" />
						<path d="M9 7.5v13" />
						<path d="M15 7.5v13" />
					</svg>
				</span>
				Artículo destacado
			</a>

			<a href="/carrito">
				<span class="footer-icon" aria-hidden="true">
					<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
						<path d="M3 4.5h2l2.2 10.4A2 2 0 0 0 9.2 16.5h8.9a2 2 0 0 0 1.9-1.4l1.5-5.1H6.2" />
						<circle cx="10" cy="20" r="1.4" />
						<circle cx="18" cy="20" r="1.4" />
					</svg>
				</span>
				Carrito
			</a>
		</div>
	</div>
</footer>

<style>
	.site-footer {
		padding: 34px 0 54px;
		border-top: 1px solid rgba(16, 16, 16, 0.08);
		background: rgba(255, 253, 249, 0.72);
		backdrop-filter: blur(18px);
		box-shadow: 0 -12px 30px rgba(16, 16, 16, 0.03);
	}

	.footer-inner {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 28px;
	}

	.footer-brand {
		display: grid;
		gap: 8px;
	}

	.footer-copy {
		max-width: 36ch;
		margin: 0;
		color: var(--muted);
	}

	.footer-links {
		display: flex;
		flex-wrap: wrap;
		justify-content: flex-end;
		gap: 12px;
	}

	.footer-links a {
		display: inline-flex;
		align-items: center;
		gap: 10px;
		min-height: 44px;
		padding: 0 15px;
		border: 1px solid rgba(16, 16, 16, 0.08);
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.5);
		color: var(--muted);
		font-size: 0.92rem;
		font-weight: 700;
		letter-spacing: 0.01em;
		box-shadow: 0 10px 20px rgba(16, 16, 16, 0.03);
		transition:
			background-color 180ms ease,
			color 180ms ease,
			transform 180ms ease,
			border-color 180ms ease,
			box-shadow 180ms ease;
	}

	.footer-links a:hover {
		background: #fff;
		color: var(--text);
		border-color: rgba(16, 16, 16, 0.14);
		transform: translateY(-1px);
		box-shadow: 0 14px 28px rgba(16, 16, 16, 0.07);
	}

	.footer-icon {
		display: inline-grid;
		place-items: center;
		width: 18px;
		height: 18px;
		flex: 0 0 auto;
		color: currentColor;
	}

	.footer-icon svg {
		width: 18px;
		height: 18px;
	}

	@media (max-width: 720px) {
		.footer-inner {
			flex-direction: column;
		}

		.footer-links {
			justify-content: flex-start;
		}
	}

	@media (max-width: 480px) {
		.footer-links a {
			width: 100%;
			justify-content: flex-start;
		}
	}
</style>
