---
import Layout from '../../layouts/Layout.astro';
import { products } from '../../data/products';
import ProductAction from '../../components/ProductAction.jsx';
import "../../styles/components/button-selector.css";
import "../../styles/components/quantity-picker.css";

export async function getStaticPaths() {
	return products.map((product) => ({
		params: { id: product.id },
		props: { product }
	}));
}

const { product } = Astro.props as {
	product: (typeof products)[number];
};

const displayPrice = product.discountPrice ?? product.price;
const hasDiscount = Boolean(product.discountPrice);
const availableColors = product.colors;
const defaultQuantity = 1;
---

<Layout title={`Tienda Ropa | ${product.name}`} description={product.description}>
	<section class="section">
		<div class="container detail-layout">
			<div class="gallery surface">
				<div class="gallery-main">
					<img src={product.images[0]} alt={product.name} />
				</div>

				<div class="gallery-thumbs">
					{product.images.map((image, index) => (
						<div class="thumb">
							<img src={image} alt={`${product.name} image ${index + 1}`} />
						</div>
					))}
				</div>
			</div>

			<div class="info stack">
				<p class="kicker">{product.category}</p>

				<div class="title-block">
					<h1 class="h2">{product.name}</h1>
					<div class="price-row">
						<span class="price">€{displayPrice}</span>
						{hasDiscount && <span class="old-price">€{product.price}</span>}
					</div>
				</div>

				<p class="lead">{product.description}</p>

				<div class="detail-panel surface-soft">
					<div class="detail-row">
						<span class="label">Material</span>
						<span>{product.material}</span>
					</div>

					<ProductAction client:load product={product} />
				</div>
				
				<p class="product-feedback" data-feedback aria-live="polite"></p>

				<div class="product-notes">
					<p class="muted">Model wears a size M. Designed for a relaxed, contemporary fit.</p>
				</div>
			</div>
		</div>
	</section>

	<script id="product-data" type="application/json" set:html={JSON.stringify(product)}></script>
	<script>
		import '../../scripts/product-detail';
	</script>
</Layout>

<style>
	.detail-layout {
		display: grid;
		grid-template-columns: minmax(0, 1.15fr) minmax(0, 0.85fr);
		gap: 32px;
		align-items: start;
	}

	.gallery {
		padding: 18px;
		display: grid;
		gap: 16px;
	}

	.gallery-main {
		overflow: hidden;
		border-radius: calc(var(--radius) - 10px);
		background: #e9e2d8;
	}

	.gallery-main img {
		width: 100%;
		aspect-ratio: 4 / 5;
		object-fit: cover;
	}

	.gallery-thumbs {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 14px;
	}

	.thumb {
		overflow: hidden;
		border-radius: 16px;
		background: #e9e2d8;
	}

	.thumb img {
		width: 100%;
		aspect-ratio: 1 / 1;
		object-fit: cover;
	}

	.info {
		padding-top: 8px;
	}

	.title-block {
		display: grid;
		gap: 12px;
	}

	.price-row {
		display: flex;
		align-items: baseline;
		gap: 12px;
	}

	.price {
		font-size: 1.9rem;
		font-weight: 700;
		letter-spacing: -0.04em;
	}

	.old-price {
		color: var(--muted);
		text-decoration: line-through;
		font-size: 1rem;
	}

	.detail-panel {
		display: grid;
		gap: 18px;
		padding: 22px;
	}

	.detail-row {
		display: grid;
		gap: 10px;
	}

	.action-row {
		display: flex;
		flex-wrap: wrap;
		gap: 12px;
		padding-top: 6px;
	}

	.product-notes {
		padding-top: 8px;
		min-height: 24px;
	}

	.product-feedback {
		min-height: 24px;
		font-size: 0.92rem;
		font-weight: 600;
		color: var(--muted);
	}

	.product-feedback[data-state='success'] {
		color: #1f6b3a;
	}

	.product-feedback[data-state='error'] {
		color: #a13f1e;
	}

	@media (max-width: 900px) {
		.detail-layout {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 640px) {
		.gallery,
		.detail-panel {
			padding: 16px;
		}

		.price {
			font-size: 1.6rem;
		}
	}
</style>
